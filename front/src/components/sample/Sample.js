import styled from 'styled-components';

const SampleBlock = styled.div``

const Sample = () => {
    return (
        <SampleBlock>
            
        </SampleBlock>
    );
}

export default Sample;